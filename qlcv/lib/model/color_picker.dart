import 'dart:ui';

class ColorPicker {
  // Base colors
  static const background = Color(0xffffffff);
  static const backgroundLight = Color(0xfff8fafc);
  static const accent = Color(0xff3b82f6); // Modern blue
  static const primary = Color(0xffffffff);
  static const primaryDark = Color(0xff1e293b);
  
  // Font colors
  static const fontDark = Color(0xff0f172a);
  static const fontMedium = Color(0xff475569);
  static const fontLight = Color(0xff94a3b8);
  
  // Status colors
  static const statusInProgress = Color(0xff3b82f6); // Blue
  static const statusCompleted = Color(0xff16a34a); // Green
  static const statusLate = Color(0xfff43f5e); // Red
  
  // UI element colors
  static const cardBackground = Color(0xffffffff);
  static const cardBorder = Color(0xffe2e8f0);
  static const buttonPrimary = Color(0xff3b82f6);
  static const buttonSecondary = Color(0xff64748b);
  static const buttonDanger = Color(0xffef4444);
  static const buttonSuccess = Color(0xff10b981);
  
  // Legacy colors (for backward compatibility)
  static const second = Color(0xfff59e0b);
  static const third = Color(0xff64748b);
  static const dark = Color(0xff991b1b);
}